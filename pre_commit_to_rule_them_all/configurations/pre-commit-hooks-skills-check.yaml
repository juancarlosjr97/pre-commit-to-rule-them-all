---
repos:
  - repo: local
    hooks:
      - id: skills-check
        name: skills check
        description: Check that agent skills are up-to-date using npx skills check.
        entry: npx --yes skills check
        language: system
        files: ^(\.agents/skills/|skills\.lock\.json)
        pass_filenames: false
...
